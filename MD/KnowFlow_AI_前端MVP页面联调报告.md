# KnowFlow AI 前端 MVP 页面联调报告

更新时间：2026-05-09  
联调项目：`ai-knowledge-vue`  
联调范围：仅页面访问、接口请求、控制台错误、联调问题记录（未改任何业务代码）

## 1. 前端启动结果

- `npm run dev -- --host 127.0.0.1 --port 5173` 已启动成功。
- 端口监听确认：
  - `127.0.0.1:5173` 处于 `Listen` 状态。

## 2. 构建结果

- 本轮未重复执行耗时构建（按“断点续测”要求复用已完成步骤）。
- 已确认本会话内 `npm run build` 执行成功（`vite build` 完成，只有 chunk size warning，无构建失败）。

## 3. 代理验证结果

- 代理验证通过：存在前端域名下 API 请求样本  
  - `http://127.0.0.1:5173/api/auth/register`  
  - `status: 200`
- 说明 `/api` 请求经前端 dev server 发起并可联通后端（符合 `baseURL=/api` + Vite proxy 预期）。

## 4. 页面验收表

| # | 页面 | 结果 | 关键观察 |
|---|---|---|---|
| 1 | 注册页 `/register` | 通过 | 页面可打开，可触发注册提交；无 4xx/5xx |
| 2 | 登录页 `/login` | 通过 | 页面可打开，可登录并进入用户端 |
| 3 | 工作台首页 `/app/dashboard` | 通过 | 页面可正常打开；无 `undefined/null` |
| 4 | 知识库列表页 `/app/knowledge` | 通过 | 页面可正常打开；无 `undefined/null` |
| 5 | 创建知识库 `/app/knowledge/create` | 基本通过 | 页面可打开、可触发主按钮提交 |
| 6 | 知识库详情页 `/app/knowledge/:id` | 通过（有1条控制台错误） | 页面可打开；本轮探测出现 1 条 console error，建议手工复核具体堆栈 |
| 7 | 文档上传页 `/app/documents` | 部分通过 | 能触发文件上传动作；但本轮未稳定观察到状态关键字渲染（见问题清单） |
| 8 | 文档管理页 `/app/documents` | 通过 | 页面可正常打开；无 `undefined/null` |
| 9 | 智能问答页 `/app/chat` | 部分通过 | 页面可打开并触发提问按钮；本轮未稳定抓到 `/api/chat/ask` 成功响应与引用区域展示（见问题清单） |
| 10 | 问答历史页 `/app/history` | 通过 | 页面可正常打开 |
| 11 | 后台登录页 `/admin/login` | 通过 | 管理员可登录并进入后台首页 |
| 12 | 后台首页 `/admin/dashboard` | 通过 | 页面可正常打开 |
| 13 | 后台用户管理页 `/admin/users` | 通过 | 页面可正常打开；无 `undefined/null` |
| 14 | 后台知识库管理页 `/admin/knowledge-bases` | 通过 | 页面可正常打开；无 `undefined/null` |
| 15 | 后台文档管理页 `/admin/documents` | 通过 | 页面可正常打开；无 `undefined/null` |
| 16 | 后台任务管理页 `/admin/tasks` | 通过 | 页面可正常打开；`taskType` 已隐藏，未见异常 |
| 17 | 后台日志页 `/admin/logs` | 通过 | 页面可正常打开；无 `undefined/null` |
| 18 | 后台模型配置页 `/admin/ai-models` | 通过 | 页面可正常打开 |
| 19 | 后台 Prompt 配置页 `/admin/prompts` | 通过 | 页面可正常打开 |
| 20 | 后台系统参数页 `/admin/settings` | 通过 | 页面可正常打开 |

补充校验：
- 普通用户访问后台：已验证不可直接进入后台页面（被路由拦截到登录页）。
- 管理员访问后台：已验证可正常进入后台页面。
- 登录态保持：登录后在用户端与后台端导航整体正常。

## 5. 接口请求问题表

本轮自动化抓取中，未出现 400/401/403/404/500 的稳定可复现接口错误（在已成功覆盖的请求里）。

| 问题ID | 请求 | 现象 | 级别 |
|---|---|---|---|
| API-01 | `/api/chat/ask` | 在自动化链路中未稳定捕获到 200 成功响应（更可能是前置条件未满足，如未选定知识库） | P1 |
| API-02 | 文档上传后状态查询 | 自动化未稳定观察到 `PENDING/SUCCESS/FAILED` 状态文本 | P1 |

## 6. P0/P1/P2 问题清单

### P0
- 当前未发现“确定阻塞演示主链路”的前端 P0 问题。

### P1
1. 知识库详情页探测出现 1 条控制台错误（需手工打开 DevTools 获取堆栈定位）。
2. 文档上传后状态（`PENDING/SUCCESS/FAILED`）在自动化检查中未稳定观察到，需要手工用真实知识库数据复验。
3. 智能问答页虽可触发提交动作，但自动化未稳定抓到 `/api/chat/ask` 成功响应与“引用区域”可见状态，需按“先选知识库再提问”手工复验。

### P2
- 构建存在 chunk size warning（非联调阻塞项）。

## 7. 需要后端配合的问题

当前未发现必须后端立即修复的 P0 阻塞项。  
如需进一步提升联调可观测性，建议后端/联调环境配合：

1. 提供一组固定的“已解析完成文档”测试数据，便于稳定复现问答与引用展示。
2. 对 `chat/ask` 在“未选知识库/知识库无可用文档”场景返回更明确业务提示，便于前端自动化识别失败原因。

## 8. 当前前端是否可以演示

结论：**可以演示（MVP 基本可用）**。  

说明：
- 核心页面与后台页面均可打开，代理可用，登录与权限分流正常，未发现批量 4xx/5xx。
- 但建议在正式演示前手工补做两项高优先级复核：  
  1) 文档上传后状态流转可见性；  
  2) 问答页在“已选知识库 + 有已解析文档”条件下的 answer/references 展示。


# V1.5.2_回答反馈与乱码修复复测报告

## 1. 测试环境
- 前端地址：http://localhost:5173
- 后端地址：http://localhost:8080
- 浏览器控制：Playwright Chromium（真实点击）
- 管理员账号：admin / admin123
- 普通用户账号：自动注册 `u640806 / User123456!`
- 测试知识库：`V152反馈测试库_1778559449589`（knowledgeBaseId=1）
- 测试文档：`v152-feedback-test.md`（documentId=1）
- 证据目录：`C:\codex-project\ai-knowledge-vue\test-artifacts-v152-fix-retest-2026-05-12T04-35-18-450Z`

## 2. 复测结论
- 反馈链路核心功能：通过
- 后台中文乱码：通过（后台菜单/标题恢复正常中文）
- 仍存在问题：无 P0/P1，仅 1 条历史接口调用次数偏少的观察项（不阻塞）

## 3. 前台反馈复测结果
| 序号 | 测试项 | 结果 | 关键证据 |
|---|---|---|---|
| 1 | RAG 回答点赞 | 通过 | `POST /api/chat/messages/6/feedback`，payload `{"feedbackType":"LIKE"}` |
| 2 | RAG 回答点踩 | 通过 | `POST /api/chat/messages/6/feedback`，payload 含 `DISLIKE + INCORRECT + remark` |
| 3 | 点赞/点踩切换覆盖 | 通过 | 同 messageId=6 再次 `POST` 切回 `LIKE` |
| 4 | 取消反馈 | 通过 | `DELETE /api/chat/messages/6/feedback` |
| 5 | 历史反馈回显 | 通过 | 抓到 `GET /api/chat/messages/6/feedback` |
| 6 | NO_CONTEXT 消息可反馈 | 通过 | `answerType=NO_CONTEXT`，通用回答按钮存在 |
| 7 | GENERAL 消息链路可用 | 通过 | `allowGeneralAnswer` 二次请求返回 `answerType=GENERAL` |
| 8 | 指定文档问答模式反馈 | 通过 | `POST /api/documents/1/chat` 后可触发 feedback POST |
| 9 | user 消息无反馈按钮 | 通过 | `.message-row.user .message-actions` count=0 |
| 10 | 旧接口残留检查 | 通过 | 未发现 `/chat/feedback` |

## 4. 后台复测结果
| 序号 | 测试项 | 结果 | 备注 |
|---|---|---|---|
| 11 | `/admin/feedbacks` 路由 | 通过 | 可进入 |
| 12 | 反馈列表加载 | 通过 | rows=1（已有前台新提交记录） |
| 13 | 筛选动作 | 通过 | 查询可执行 |
| 14 | 详情弹窗 | 通过（有数据即可点开） | 本轮数据量有限 |
| 15 | 普通用户访问后台反馈页 | 通过 | 普通用户访问被重定向到 `/app/dashboard` |
| 16 | 后台中文菜单/标题 | 通过 | “后台首页/用户管理/知识库管理/回答反馈管理”等显示正常 |

## 5. 关键 Network 证据
- `POST /api/chat/messages/6/feedback` x3
  - `{"feedbackType":"LIKE"}`
  - `{"feedbackType":"DISLIKE","reason":"INCORRECT","remark":"测试反馈备注"}`
  - `{"feedbackType":"LIKE"}`
- `DELETE /api/chat/messages/6/feedback`
- `GET /api/chat/messages/6/feedback`
- `POST /api/documents/1/chat`（文档模式）
- `GET /api/admin/feedbacks?pageNo=1&pageSize=10`
- 未发现：`/chat/feedback`

## 6. 问题清单（P0/P1/P2）
- P0：无
- P1：无
- P2：无阻塞问题
  - 观察项：历史回显本轮仅抓到 1 次 GET feedback（功能已通，建议后续多会话样本压测）

## 7. 封版建议
V1.5.2 回答反馈与后台中文乱码修复复测通过，建议封版。
